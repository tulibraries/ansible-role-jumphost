---
# defaults file for ansible-role-jumphost

jumphost_user: "{{ ansible_user }}"

# SSH config location for jumphost
jumphost_ssh_dir: "{{ ansible_env.HOME }}/.ssh"
jumphost_ssh_config_path: "{{ jumphost_ssh_dir }}/config"
